server:
  port: 10102
  
spring:
  application: 
    name: eurake-hello  
  cloud:
    discovery:
      enabled: true
    config:
      uri: http://localhost:8888/
      profile: hello2
      
eureka:
  client:  
    serviceUrl:
      defaultZone: http://localhost:9999/eureka/  

